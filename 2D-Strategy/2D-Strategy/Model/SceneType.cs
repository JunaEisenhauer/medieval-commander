namespace StrategyGame.Model
{
    /// <summary>
    /// Enum for the different scene types. Exit is the end of the game.
    /// </summary>
    public enum SceneType
    {
        Menu,
        Game,
        Exit,
    }
}
